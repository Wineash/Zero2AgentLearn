# LLM API Playground

用于测试 LLM API 调用的实验项目。

## 快速开始

1. 创建虚拟环境并安装依赖：
   ```bash
   python -m venv venv
   source venv/bin/activate  # Linux/macOS
   venv\Scripts\activate   # Windows
   pip install -r requirements.txt